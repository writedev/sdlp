name: Publish

on:
  push:
    tags:
      - "v*"

jobs:
  publish:
    runs-on: ubuntu-latest

    permissions:
      id-token: write   # obligatoire pour Trusted Publishing
      contents: read

    environment: pypi  # doit matcher PyPI si configuré

    steps:
      - uses: actions/checkout@v4

      - name: Install uv
        uses: astral-sh/setup-uv@v1

      - name: Build
        run: uv build

      - name: Publish
        run: uv publish
